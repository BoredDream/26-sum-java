@startuml
title 学生考勤模块流程图
start
:教师/管理员发布签到任务;
:系统校验任务时间、类型和适用范围;
if (发布校验通过?) then (否)
  :提示修改签到任务信息;
  stop
else (是)
endif

:系统为范围内学生生成缺勤记录;
:学生查看进行中签到任务;

if (是否在签到时间内?) then (是)
  :学生点击在线签到;
  if (是否需要定位校验?) then (是)
    :获取当前位置;
    if (是否在签到范围内?) then (否)
      :签到失败并提示原因;
      :记录保持缺勤;
    else (是)
      :系统校验是否重复签到;
      if (是否已签到?) then (是)
        :提示已完成签到;
      else (否)
        :系统判定正常或迟到;
        :保存签到时间和状态;
      endif
    endif
  else (否)
    :系统校验是否重复签到;
    if (是否已签到?) then (是)
      :提示已完成签到;
    else (否)
      :系统判定正常或迟到;
      :保存签到时间和状态;
    endif
  endif
else (否)
  :任务结束后记录保持缺勤;
endif

if (是否需要补签?) then (是)
  :学生提交补签申请;
  :教师/管理员查看待审核申请;
  if (审核是否通过?) then (是)
    :考勤记录状态改为补签;
  else (否)
    :补签申请驳回，记录保持原状态;
  endif
else (否)
endif

:学生查看本人考勤记录;
:教师/管理员查询考勤记录;
:系统生成考勤统计;
:教师/管理员导出考勤报表;
stop
@enduml
